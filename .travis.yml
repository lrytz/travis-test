language: scala

scala:
  - 0.18.1-RC1
  - 2.12.10
  - 2.13.0

env:
  - ADOPTOPENJDK=8     SCALAJS_VERSION=
  - ADOPTOPENJDK=8     SCALAJS_VERSION=0.6.28
  - ADOPTOPENJDK=8     SCALAJS_VERSION=1.0.0-M8
  - ADOPTOPENJDK=11    SCALAJS_VERSION=

matrix:
  exclude:
    - scala: 0.18.1-RC1
      env: ADOPTOPENJDK=8     SCALAJS_VERSION=0.6.28
    - scala: 0.18.1-RC1
      env: ADOPTOPENJDK=8     SCALAJS_VERSION=1.0.0-M8

before_install:
  # adding $HOME/.sdkman to cache would create an empty directory, which interferes with the initial installation
  - "[[ -d $HOME/.sdkman/bin/ ]] || rm -rf $HOME/.sdkman/"
  - curl -sL https://get.sdkman.io | bash
  - echo sdkman_auto_answer=true > $HOME/.sdkman/etc/config
  - source "$HOME/.sdkman/bin/sdkman-init.sh"

install:
  - sdk install java $(sdk list java | grep -o "$ADOPTOPENJDK\.[0-9\.]*hs-adpt" | head -1)
  - unset JAVA_HOME
  - java -Xmx32m -version
  - javac -J-Xmx32m -version
  - git fetch --tags # get all tags for sbt-dynver

script:
  - if [[ "$ADOPTOPENJDK" == "8" && "$TRAVIS_SCALA_VERSION" =~ 2\.1[123]\..* ]]; then export RELEASE=true; fi
  - ./build.sh

before_cache:
  - find $HOME/.sbt -name "*.lock" | xargs rm
  - find $HOME/.ivy2 -name "ivydata-*.properties" | xargs rm
cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot
    - $HOME/.sbt/launchers
    - $HOME/.sdkman
